import { Container } from "@mui/material";
import React, { useState } from "react";
export default function RandomPoke(){
const [random, setRandom]=useState(null);

// get pokemon random
// get usuario ?? donde esta? ya esta en el codigo
// mutate que haga el registro en el backend

// backend; api, migracion y la funcion en el repo.

return(
<Container>
    card que muestre el pokemon
boton que tenga; atrapar o dejar pokemon
</Container>)
}